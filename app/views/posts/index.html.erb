<% @posts.each do |post| %>
		<div class="genie-box text-center col-xs-10 col-xs-offset-1 col-sm-2.5 col-sm-offset-1">
			<h1>
		<%=post.title %>
			</h1>
			<h5>
			<%=post.description %>
		</h5>
		</div>
	<% end %>

